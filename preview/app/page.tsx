export default function PreviewPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-[var(--color-neutral-500)]">
        Add components to <code>src/components/</code> and import them here.
      </p>
    </main>
  );
}
